# Database (SQLite for local, update path as needed)
DATABASE_URL="file:./prisma/dev.db"

# Server port
PORT=5000

# CORS — comma-separated list of allowed frontend origins
# Example for production: CORS_ORIGIN=https://your-app.vercel.app
CORS_ORIGIN=http://localhost:3000
